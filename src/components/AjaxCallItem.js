import React from 'react';
import axios from "axios";
import VideoListItem from "./VideoListItem";

const AjaxCallItem = ({video}) => {

    var videoItems = "";
    //video.map((video) => {
    //    videoItems = videoItems + video.videoID + ", ";
    //});


    return (
        <div>sto cazzo</div>
    );
};

export default AjaxCallItem;